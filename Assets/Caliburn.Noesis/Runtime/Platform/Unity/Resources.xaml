<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:cal="clr-namespace:Caliburn.Noesis"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--  DataTemplate.QueryDialog.Implicit  -->
    <DataTemplate DataType="{x:Type cal:QueryDialog}">
        <DataTemplate.Resources>
            <cal:EnumFlagSetToVisibilityConverter x:Key="Caliburn.Converter.EnumFlagSetToVisible" />
            <cal:IsEqualConverter x:Key="Caliburn.Converter.IsEqualTo" />
        </DataTemplate.Resources>
        <DockPanel Margin="20">
            <TextBlock
                DockPanel.Dock="Top"
                FontSize="17"
                HorizontalAlignment="Center"
                Text="{Binding Title, Mode=OneWay}"
                TextAlignment="Center" />
            <UniformGrid
                DockPanel.Dock="Bottom"
                HorizontalAlignment="Center"
                Rows="1"
                VerticalAlignment="Bottom">
                <Button
                    Content="OK"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Ok}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Ok}}"
                    cal:CloseDialog.Result="Ok" />
                <Button
                    Content="Yes"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Yes}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Yes}}"
                    cal:CloseDialog.Result="Yes" />
                <Button
                    Content="No"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.No}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.No}}"
                    cal:CloseDialog.Result="No" />
                <Button
                    Content="Retry"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Retry}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Retry}}"
                    cal:CloseDialog.Result="Retry" />
                <Button
                    Content="Try Again"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.TryAgain}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.TryAgain}}"
                    cal:CloseDialog.Result="TryAgain" />
                <Button
                    Content="Abort"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Abort}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Abort}}"
                    cal:CloseDialog.Result="Abort" />
                <Button
                    Content="Continue"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Continue}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Continue}}"
                    cal:CloseDialog.Result="Continue" />
                <Button
                    Content="Ignore"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Ignore}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Ignore}}"
                    cal:CloseDialog.Result="Ignore" />
                <Button
                    Content="Cancel"
                    IsDefault="{Binding DefaultResult, Mode=OneWay, Converter={StaticResource Caliburn.Converter.IsEqualTo}, ConverterParameter={x:Static cal:DialogResult.Cancel}}"
                    Margin="10,0"
                    MinWidth="50"
                    Visibility="{Binding DialogResults, Mode=OneWay, Converter={StaticResource Caliburn.Converter.EnumFlagSetToVisible}, ConverterParameter={x:Static cal:DialogResults.Cancel}}"
                    cal:CloseDialog.Result="Cancel" />
            </UniformGrid>
            <TextBlock
                HorizontalAlignment="Center"
                Margin="0,20"
                Text="{Binding Content, Mode=OneWay}"
                TextAlignment="Center"
                VerticalAlignment="Center" />
        </DockPanel>
    </DataTemplate>

    <!--  Caliburn.ControlTemplate.DialogItem  -->
    <ControlTemplate TargetType="{x:Type cal:DialogItem}" x:Key="Caliburn.ControlTemplate.DialogItem">
        <Border
            Background="{TemplateBinding Background}"
            BorderBrush="{TemplateBinding BorderBrush}"
            BorderThickness="{TemplateBinding BorderThickness}"
            CornerRadius="{TemplateBinding Border.CornerRadius}"
            Padding="{TemplateBinding Padding}"
            RenderTransformOrigin=".5,.5"
            TextElement.Foreground="{TemplateBinding Foreground}"
            x:Name="Border">
            <ContentPresenter ContentSource="Content" />
        </Border>
    </ControlTemplate>

    <!--  Caliburn.ControlTemplate.DialogHost  -->
    <ControlTemplate TargetType="{x:Type cal:DialogHost}" x:Key="Caliburn.ControlTemplate.DialogHost">
        <Grid
            Background="{TemplateBinding OverlayBackgroundBrush}"
            Visibility="Collapsed"
            x:Name="PART_RootGrid">
            <cal:DialogItem
                Content="{TemplateBinding Content}"
                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                Style="{TemplateBinding DialogContainerStyle}"
                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
        </Grid>
    </ControlTemplate>

    <!--  Caliburn.Style.DialogItem  -->
    <Style TargetType="{x:Type cal:DialogItem}" x:Key="Caliburn.Style.DialogItem">
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="KeyboardNavigation.ControlTabNavigation" Value="Cycle" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Cycle" />
        <Setter Property="MinWidth" Value="300" />
        <Setter Property="MinHeight" Value="150" />
        <Setter Property="Border.CornerRadius" Value="0" />
        <Setter Property="Foreground" Value="#E4E6E8" />
        <Setter Property="Background" Value="#262E35" />
        <Setter Property="BorderBrush" Value="#FF1098DF" />
        <Setter Property="BorderThickness" Value="0,5" />
        <Setter Property="Template" Value="{StaticResource Caliburn.ControlTemplate.DialogItem}" />
    </Style>

    <!--  Caliburn.Style.DialogHost  -->
    <Style TargetType="{x:Type cal:DialogHost}" x:Key="Caliburn.Style.DialogHost">
        <Setter Property="OverlayBackgroundBrush">
            <Setter.Value>
                <RadialGradientBrush Center="0.5, 0.5">
                    <GradientStop Color="#AA000000" Offset="0.0" />
                    <GradientStop Color="#CC000000" Offset="1.0" />
                </RadialGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="DialogContainerStyle" Value="{StaticResource Caliburn.Style.DialogItem}" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template" Value="{StaticResource Caliburn.ControlTemplate.DialogHost}" />
    </Style>

    <!--  Style.DialogItem.Implicit  -->
    <Style BasedOn="{StaticResource Caliburn.Style.DialogItem}" TargetType="{x:Type cal:DialogItem}" />

    <!--  Style.DialogHost.Implicit  -->
    <Style BasedOn="{StaticResource Caliburn.Style.DialogHost}" TargetType="{x:Type cal:DialogHost}" />

</ResourceDictionary>
